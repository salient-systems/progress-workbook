<%= form_for(@assessment_grade) do |f| %>
  <% if @assessment_grade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assessment_grade.errors.count, "error") %> prohibited this assessment_grade from being saved:</h2>

      <ul>
      <% @assessment_grade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :total %><br>
    <%= f.text_field :total %>
  </div>
  <div class="field">
    <%= f.label :assessment_id %><br>
    <%= f.number_field :assessment_id %>
  </div>
  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.number_field :student_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
